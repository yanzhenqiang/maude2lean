import Nat

-- Constant function: returns first argument
theorem const_nat : forall (n : Nat), Nat -> Nat := by
  intro n m
  exact n

-- Identity function on Nat
theorem id_nat : forall (n : Nat), Nat -> Nat := by
  intro n m
  exact m
